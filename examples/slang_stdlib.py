# STDLIB

## Constants
_SPC    =   " "
_NWL    =   "\n"

## Functions
def equals(a, b)    -> bool: return a == b
def less(a, b)      -> bool: return a < b
def greater(a, b)   -> bool: return a > b

def inc(a, b = 1) -> int: return a+b
def dec(a, b = 1) -> int: return a-b
